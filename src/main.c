#include "shell.h"
#include "parser.h"
#include "executor.h"

int main() {
    // Initialize any shell components if needed
    
    // Enter the main shell loop
    shell_loop();
    
    return 0;
}