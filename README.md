# Technical Documentationexec
ls -l              # List files
exec sleep 5 &          # Run sleep in background
globalusage            # Show version
exec ps > output.txt   # Redirect output to file
quit                   # Exit the shell